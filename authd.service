[Unit]
Description=Authentication daemon for privilege escalation
After=local-fs.target

[Service]
Type=simple
ExecStart=/usr/local/bin/authd
Restart=on-failure
RestartSec=1

[Install]
WantedBy=multi-user.target
